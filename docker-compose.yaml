services:
  guardrails:
    build: .
    image: ghcr.io/zephyrcodesstuff/guardrails:latest
    container_name: guardrails
    ports:
      - "53:8053/udp"
      - "53:8053/tcp"
    environment:
      - CONFIG_PATH=/app/config.toml
    volumes:
      - ./config.toml:/app/config.toml:ro